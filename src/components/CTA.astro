---
// src/components/CTA.astro
---

<section class="relative py-24 px-6 md:px-12 bg-white dark:bg-gray-900 overflow-hidden">
  <!-- Fondo degradado difuminado -->
  <div
    class="
      absolute inset-0
      bg-gradient-to-tr from-transparent via-cm-400 to-transparent
      opacity-20
      blur-3xl
      pointer-events-none
    "
    aria-hidden="true"
  ></div>

  <!-- Contenido centrado -->
  <div
    class="
      relative z-10
      container mx-auto max-w-xl
      flex flex-col items-center text-center
      space-y-6
      animate-fade-up
    "
  >
    <h2 class="text-4xl md:text-5xl font-bold text-gray-900 dark:text-gray-100">
      El siguiente nivel en formación sólo es posible con el aliado correcto.
    </h2>
    <p class="text-lg text-gray-700 dark:text-gray-300 max-w-lg">
      Aumenta tus indicadores de gestión al contratar un aliado para tu empresa.
    </p>
    <div class="group">
      <a
        id="hero-button"
        href="#form-container"
        class="
          relative inline-flex items-center justify-center 
          bg-gradient-to-r from-cm-500 to-teal-500
          text-white font-semibold text-lg 
          py-3 px-8 rounded-lg 
          shadow-lg 
          overflow-hidden
          transition-transform duration-300 ease-in-out
          hover:scale-105 hover:shadow-2xl
        "
        aria-label="Reserva tu lugar ahora"
      >
        <span class="relative z-10">Reserva tu lugar ahora</span>
      </a>
    </div>
  </div>
</section>

<style>
  /* Animación de fade-up */
  @keyframes fade-up {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }
  .animate-fade-up {
    animation: fade-up 0.8s ease-out both;
  }
</style>
